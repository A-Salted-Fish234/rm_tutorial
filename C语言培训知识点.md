# C语言第一节课知识点

- [ ] **编译**
- [ ] **预处理指令**

​	以 `#` 开头的命令，在**编译之前**执行。主要用于包含文件 (`#include`)、定义常量 (`#define`)、条件编译 (`#ifdef`)。

**C语言常用预处理指令列表**

| 预处理指令                        | 类别       | 说明与主要用途                                               | 示例                                                      |
| :-------------------------------- | :--------- | :----------------------------------------------------------- | :-------------------------------------------------------- |
| **`#include`**                    | 文件包含   | **将指定文件的内容复制并插入到当前指令的位置。** 用于包含头文件，实现代码复用。 | `#include <stdio.h>` `#include "myheader.h"`              |
| **`#define`**                     | 宏定义     | **定义宏（符号常量或宏函数）。** 用于定义常量、简化代码或实现轻量级函数。 | `#define PI 3.14159` `#define MAX(a,b) ((a)>(b)?(a):(b))` |
| **`#undef`**                      | 宏定义     | **取消已定义的宏。** 用于移除一个已有的宏定义。              | `#undef MAX`                                              |
| **`#ifdef`** **`#ifndef`**        | 条件编译   | **条件编译检查。** 用于根据某个宏是否被定义来包含或排除代码块。 `#ifdef`：如果宏已定义，则编译后续代码。 `#ifndef`：如果宏未定义，则编译后续代码。 | `#ifdef DEBUG` `#endif`  `#ifndef HEADER_H` `#endif`      |
| **`#if`** **`#elif`** **`#else`** | 条件编译   | **条件编译。** 用于更复杂的条件判断，可以计算常量表达式。    | `#if VERSION > 5` `#elif VERSION > 3` `#else` `#endif`    |
| **`#endif`**                      | 条件编译   | **结束条件编译块。** 与 `#if`, `#ifdef`, `#ifndef` 配对使用。 | (见上例)                                                  |
| **`#pragma`**                     | 编译器指令 | **提供编译器特定的指令。** 用于设置编译器选项或执行特殊操作。因编译器而异，不可移植。 | `#pragma once` `#pragma warning(disable: 4996)`           |
| **`#error`**                      | 错误诊断   | **在预处理阶段生成一个编译错误并输出消息。** 用于检查不满足的编译条件。 | `#error "Platform not supported!"`                        |
| **`#line`**                       | 行控制     | **改变预处理器中存储的行号(`__LINE__`)和文件名(`__FILE__`)。** 主要用于高级调试和代码生成工具。 | `#line 100 "myfile.c"`                                    |
| **`#`** (运算符)                  | 宏操作     | **在宏函数中，将宏参数转换为字符串字面量。**                 | `#define STR(x) #x` `STR(hello)` -> `"hello"`             |
| **`##`** (运算符)                 | 宏操作     | **在宏函数中，将两个标记连接在一起形成一个新标记。** 用于创建新的标识符。 | `#def`                                                    |

- [ ] **头文件**

​	通常包含**函数声明**、**宏定义**、**类型定义**。通过 `#include` 指令将内容引入源文件，以便使用库函数或模块接口。例如：`stdio.h`。

- [ ] **注释**

​	代码中用于解释说明的文字，**编译器会忽略**。`// 单行注释`，`/* 多行注释 */`。提高代码可读性。

- [ ] **main函数**

​	程序的**唯一入口**。操作系统执行程序时，从这里开始运行。标准形式：`int main(void)` 或 `int main(int argc, char *argv[])`。

- [ ] **代码规范**

​	编写代码的**风格约定**（如缩进、命名、括号位置）。虽然不影响程序运行，但对代码的**可读性和可维护性**至关重要。

- [ ] **变量**

​	程序中用于**存储数据**的基本单元。使用前必须**声明其数据类型**（如 `int age;`）。变量类型、变量大小。变量名规范：

- [ ] **数据类型**

​	定义了变量所存储数据的**种类**、**大小**和**解释方式**。基本类型：`int`, `float`, `double`, `char`。

- [ ] **运算符**

​	用于执行运算的符号。例如：算术运算符 `+ - * / %`，关系运算符 `> < == !=`，逻辑运算符 `&&

- [ ] **表达式**

​	由变量、常量和运算符组成的式子，用于**计算求值**。例如：`a + b * 5`。表达式本身不包含分号。

- [ ] **语句**

​	程序执行的**基本单位**，通常以分号 `;` 结尾。例如：赋值语句 `a = 10;`，控制语句 `if (...) {}`。

- [ ] **关键字**

​	[C keywords - cppreference.com](https://cppreference.com/w/c/keyword.html)

​	也被称为保留字，是C语言语法本身定义的、具有特殊含义的单词，**不能用作变量名或函数名**。例如：`int`, `if`, `for`, `return`。

**C语言关键字分类表**

| 类别               | 关键字             | 主要用途说明                                                 |
| :----------------- | :----------------- | :----------------------------------------------------------- |
| **数据类型**       | `int`              | 声明整型变量或函数返回值类型。                               |
| ^                  | `char`             | 声明字符型变量或函数返回值类型。                             |
| ^                  | `float`            | 声明单精度浮点型变量或函数返回值类型。                       |
| ^                  | `double`           | 声明双精度浮点型变量或函数返回值类型。                       |
| ^                  | `short`            | 修饰 `int`，声明短整型变量。                                 |
| ^                  | `long`             | 修饰 `int`、`double`，声明长整型或长双精度浮点型变量。       |
| ^                  | `signed`           | 修饰 `int`、`char`，声明有符号类型（默认）。                 |
| ^                  | `unsigned`         | 修饰 `int`、`char`，声明无符号类型。                         |
| ^                  | `_Bool` (C99)      | 声明布尔类型（需包含 `stdbool.h` 使用 `bool`）。             |
| ^                  | `_Complex` (C99)   | 声明复数类型。                                               |
| ^                  | `_Imaginary` (C99) | 声明虚数类型。                                               |
| **类型修饰与定义** | `const`            | 定义常量，表示其值不可修改。                                 |
| ^                  | `volatile`         | 防止编译器优化，指明变量可能被意外改变。                     |
| ^                  | `restrict` (C99)   | 给编译器优化提示，表明指针是访问其所指数据的唯一方式。       |
| ^                  | `void`             | ① 表示函数无返回值。 ② 作为函数参数，表示函数无参数 (`void`)。 ③ 声明无类型指针 (`void*`)，可指向任何数据类型。 |
| ^                  | `enum`             | 定义枚举类型。                                               |
| ^                  | `struct`           | 定义结构体类型。                                             |
| ^                  | `union`            | 定义共用体（联合体）类型。                                   |
| ^                  | `typedef`          | 为已有的数据类型定义一个新的名称（别名）。                   |
| **存储类别**       | `auto`             | 声明自动变量（默认，通常省略）。在函数内部，变量默认就是 `auto`。 |
| ^                  | `static`           | ① **静态局部变量**：使局部变量的生命周期延长至整个程序运行期。 ② **静态全局变量/函数**：限制变量或函数的作用域仅在其所在的源文件内。 |
| ^                  | `extern`           | 声明一个变量或函数是在其他文件或本文件后面定义的（外部链接）。 |
| ^                  | `register`         | 建议编译器将变量存储在CPU寄存器中以便快速访问（仅是建议，编译器可忽略）。 |
| **流程控制**       | `if`               | 条件语句，用于判断。                                         |
| ^                  | `else`             | 条件语句否定分支（与 `if` 连用）。                           |
| ^                  | `switch`           | 用于多分支选择语句。                                         |
| ^                  | `case`             | `switch` 语句中的一个分支。                                  |
| ^                  | `default`          | `switch` 语句中默认的分支。                                  |
| ^                  | `for`              | 一种循环语句。                                               |
| ^                  | `do`               | 循环语句的循环体（与 `while` 连用）。                        |
| ^                  | `while`            | ① `while` 循环。 ② `do...while` 循环的一部分。               |
| **跳转语句**       | `break`            | ① 终止当前循环（`for`, `while`, `do...while`）。 ② 跳出 `switch` 语句的一个 `case`。 |
| ^                  | `continue`         | 结束本次循环，跳过剩余语句，直接进入下一次循环判断。         |
| ^                  | `return`           | 从函数中返回，并可带一个返回值。                             |
| ^                  | `goto`             | 无条件跳转到语句标号处（一般不推荐使用，容易破坏程序结构）。 |
| **运算符**         | `sizeof`           | 计算变量或数据类型所占的内存大小（以字节为单位）。           |

- [ ] **printf、scanf**

​	C标准库中最常用的**输入输出函数**。`printf` 用于**格式化输出**到屏幕，`scanf` 用于从键盘**格式化输入**。

- [ ] **常量**

​	值在程序运行期间**不可改变**的量。例如：`const int max = 100;`, `#define PI 3.14`，或字面量 `'A'`, `100`。

- [ ] **类型转换**

​	将一种数据类型的值转换为另一种数据类型。分**隐式转换**（编译器自动完成）和**显式转换**（强制转换，如 `(float) a / b`）。

- [ ] **ASCII**

# C语言第二节课知识点

- [ ] **函数**

​	用于完成特定任务的代码块。是C程序的基本组成单元。包括**函数声明**、**函数调用**和**函数定义**。函数头、函数体、返回变量类型、参数、作用域。

- [ ] **作用域**

​	变量、函数等标识符的**有效范围**。决定了在代码的哪些地方可以访问它。主要分**局部作用域**（在 `{}` 内）和**全局作用域**。

- [ ] **数组**

​	一种数据结构，用于在**连续内存**中存储**多个相同类型**的数据元素。通过**下标**（索引）访问。例如：`int scores[10];`。

- [ ] **字符串**

​	在C语言中，字符串实际上是**以空字符 `'\0'` 结尾的字符数组**。例如：`char name[] = "Alice";`。

- [ ] **控制流**

​	控制程序代码**执行顺序**的语句。包括：条件语句 (`if-else`, `switch`)、循环语句 (`for`, `while`, `do-while`)、跳转语句 (`break`, `continue`, `return`)。

- [ ] 多文件工程写法
- [ ] Makefile





# C语言第三节课知识点

- [ ] **内存空间**

- [ ] 指针、地址

- [ ] 结构体

- [ ] 数组深入

- [ ] 函数指针

- [ ] 指针传参

  

# C语言番外课知识点



- [ ] **数据存储**

​	理解数据（尤其是不同数据类型）在计算机内存中是如何以**二进制形式**表示的。包括原码、反码、补码（用于整数）、IEEE754标准（用于浮点数）。

- [ ] 
- [ ] git
- [ ] 数据结构
- [ ] 算法